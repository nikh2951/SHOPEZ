PORT=1529
MONGO_URI=mongodb://localhost:27017/shopez
JWT_SECRET=shopez_secret_key_13579

# Configure Gmail settings for sending actual OTP emails.
# If these are empty, OTPs will be printed to the backend console logs.
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Google OAuth Client ID for Continue with Google (Optional, falls back to demo mode if empty)
GOOGLE_CLIENT_ID=
